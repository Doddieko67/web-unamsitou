# Frontend Environment Variables - Development

# Supabase Configuration
VITE_SUPABASE_URL=https://tu-proyecto.supabase.co
VITE_SUPABASE_KEY=tu_anon_public_key_de_supabase

# Backend URL
VITE_BACKEND_URL=http://localhost:3000

# Encryption Key (shared with backend for AES-256 encryption)
# Generate with: openssl rand -hex 16
VITE_ENCRYPTION_KEY=your_32_character_encryption_key_here

# IMPORTANTE:
# 1. Nunca subas el archivo .env al repositorio
# 2. Usa anon key para el frontend (no service_role_key)
# 3. Para producción, cambia VITE_BACKEND_URL a tu dominio público
# 4. VITE_ENCRYPTION_KEY debe ser la misma en frontend y backend