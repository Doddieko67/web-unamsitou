# REACTI Backend - Environment Variables
# Copia este archivo como .env y completa con tus valores reales

# Google Gemini AI
GEMINI_API_KEY=tu_api_key_de_gemini

# Supabase Configuration
SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_KEY=tu_service_role_key_de_supabase

# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend URL (para CORS)
FRONTEND_URL=http://localhost:5173

# Encryption Key (shared with frontend for AES-256 encryption)
# Generate with: openssl rand -hex 16
ENCRYPTION_KEY=your_32_character_encryption_key_here

# IMPORTANTE:
# 1. Nunca subas el archivo .env al repositorio
# 2. Usa service_role_key para el backend (no anon key)
# 3. Para producción, configura estas variables en tu servidor
# 4. El PORT debe coincidir con el Dockerfile
# 5. ENCRYPTION_KEY debe ser la misma en frontend y backend